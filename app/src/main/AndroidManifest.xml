<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:noHistory="true"
            android:name=".MainActivity"
            android:label="PROSPECTOS"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:noHistory="true"
            android:name=".ProspectoActivity"
            android:label="EDITAR PROSPECTO">
        </activity>
        <activity
            android:name=".AñadirProspecto"
            android:label="CAPTURA PROSPECTO">
        </activity>
        <activity
            android:name=".ObtenerDocumento"
            android:label="VER DOCUMENTOS ">
        </activity>
        <activity
            android:name=".VerDocumento"
            android:label="DOCUMENTOS">
        </activity>
    </application>

</manifest>